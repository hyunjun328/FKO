# 일정 및 결과 자동화 보강 작업 기록

- 2026-07-31. 이전 구현은 완료 대회 결과만 직접 수집했고, 예정 대회는 검수 파일에만 저장해 화면에 자동 반영하지 못했다.
- 이번 보강의 목표는 직접 수집한 예정 대회와 결과를 모두 화면 데이터에 연결하고, 결과 오매칭과 잘못된 라운드 표기를 방지하는 것이다.
- 결과를 찾지 못한 완료 대회는 메인 일정에 결과 확인 중 상태와 마지막 확인 시각을 표시한다.
- 직접 수집한 예정 대회는 기존에 대진과 정확한 시간이 있는 일정과 같은 날짜면 기존 데이터를 우선한다. 새 대회는 임의 시간을 만들지 않고 시간·대진 발표 대기로 표시한다.
- UFCStats는 현재 HTTPS 연결을 거부해 공개 HTTP 엔드포인트를 사용한다. 실제 수집 실행에서는 예정·완료 목록 모두 0건으로 정상 처리됐다.
- 2026-07-31. `npm test`와 `npm run test:pages`를 통과했다. 빌드, Node 테스트 25개, Python 테스트 9개가 모두 성공했다.
