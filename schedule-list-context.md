# 2026-07-31 일정 목록 가독성 결정 기록

- 왼쪽 날짜 숫자 블록은 날짜 의미를 중복해 보여 주므로 제거한다. 정확한 날짜와 시간은 각 경기 아래 일정 문장에 유지한다.
- 배지는 선수 프로필의 UFC 공식 랭킹 문자열만 사용한다. 챔피언은 `C`, 순위가 있는 선수는 숫자로 표시하고, 공식 랭킹이 없는 선수는 표시하지 않는다.
- `npm test`의 23개 테스트와 Python 7개 테스트, `npm run test:pages`의 GitHub Pages 2개 테스트를 통과했다.

## 2026-07-31 일정 목록 재구성

- 목록에는 날짜와 시간, 대회명과 개최 도시, 대표 대진, 영문 이름, 메인카드 수 순으로만 둔다. 전체 대진은 상세 화면에서 계속 제공한다.
- 대진의 양 선수 이름과 영문 이름은 `vs`를 중심으로 양끝 정렬한다.
- `npm test`와 `npm run test:pages`를 모두 통과했다. 브라우저 연결이 제공되지 않아, 서버 렌더링 HTML과 정적 내보내기 검증으로 화면 구조를 확인했다.

## 2026-07-31 공통 화면 구조

- 모든 페이지에 이미 `AppHeader`가 있으므로, 별도 헤더를 만들지 않고 이 공통 컴포넌트의 메뉴 상태와 반응형 규칙을 기준으로 통일한다.
- 1080px 이하에서는 업데이트 상태 문구를 숨기고 메뉴 영역을 가로 스크롤로 유지한다. 메뉴를 삭제하지 않아 정보 접근성은 유지한다.
- `npm test`와 `npm run test:pages`를 통과했다. 이 환경에는 연결 가능한 브라우저와 Playwright가 없어, 로컬 서버의 HTML 응답까지 확인했다.

## 2026-07-31 다음 주요 매치 분리

- 다음 대회는 대회 자체의 대표 대진과 시간만 담는 넓은 블록으로 유지한다.
- `다음 주요 매치`는 바로 아래의 독립 카드로 분리해 다른 대회 정보를 한 컨테이너에 섞지 않는다.
- `npm test`와 `npm run test:pages`를 통과했다.

## 2026-07-31 상세 정보 확대와 확인 시각

- 공개 페이지가 날짜만 표시하면 이전 캐시인지 판단하기 어려우므로 KST 시각까지 함께 표시한다.
- 목록 아래로 옮긴 상세 대진은 넓어진 화면 폭에 맞춰 메인카드 사진과 선수 정보를 키운다.
- 자동 수집 워크플로의 최근 3회 실행은 모두 성공했고 약 12시간 간격이었다. 공개 페이지도 2026년 7월 31일 상태로 배포된 것을 확인했다.

## 2026-07-31 대회 상세 패널 배치

- 대회 상세 패널은 일정 목록 아래에 전체 폭으로 두고 오른쪽 고정 패널로 사용하지 않는다.
- 목록, 달력, 주요 매치에서 대회를 선택하면 `#event-detail` 위치로 부드럽게 이동한다.
- `npm test`와 `npm run test:pages`를 통과했다.
