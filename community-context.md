# 게스트 커뮤니티 작업 메모

- 공개 가능한 Supabase 프로젝트 URL과 publishable key만 클라이언트에 넣었다.
- 게시글과 댓글 원본 테이블의 `password_hash`는 읽기 권한을 주지 않고, 안전한 열만 가진 공개 피드 뷰를 제공한다.
- 작성 RPC는 bcrypt 해시를 생성하고 `void`를 반환하므로, 해시가 응답에 노출되지 않는다.
- 댓글 대상 키는 다음 메인 이벤트에는 `event:<이벤트 ID>:main-event`, 선수에는 `fighter:<영문 이름>`을 사용한다.
- 이번 범위는 익명 게시글·댓글 작성과 조회다. 비밀번호를 이용한 수정·삭제와 대댓글은 다음 기능으로 분리한다.
