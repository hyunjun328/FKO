# 게스트 커뮤니티 작업 메모

- 공개 가능한 Supabase 프로젝트 URL과 publishable key만 클라이언트에 넣었다.
- 게시글, 댓글, 예측 원본 테이블의 민감한 열에는 읽기 권한을 주지 않고 필요한 공개 피드·집계 뷰만 제공한다.
- 작성 RPC는 bcrypt 해시를 생성하고 `void`를 반환하므로 게시글·댓글 비밀번호 해시가 응답에 노출되지 않는다.
- 댓글 대상 키는 모든 이벤트에 `event:<이벤트 ID>:main-event`, 선수에는 `fighter:<영문 이름>`을 사용한다.
- 예측 대상 키는 `event:<이벤트 ID>:bout:<좌측 영문 이름>-<우측 영문 이름>`이다. 브라우저 UUID당 대진 하나에 한 표이며 선택은 변경할 수 있다.
- 이번 범위는 익명 게시글·댓글 작성과 조회, 메인카드 승부예측이다. 비밀번호를 이용한 수정·삭제와 대댓글은 다음 기능으로 분리한다.
