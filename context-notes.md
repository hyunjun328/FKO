# 작업 맥락 기록

## 2026-07-29

- 기존 뉴스 프로그램은 유지하고 `fight-calendar`라는 별도 프로젝트로 분리했다.
- 1차는 일정 확인 도구에 집중한다.
- 로그인, 랜덤 아이디, 승부예측, 댓글, 순위, 관심 경기, 스포일러, 외부 캘린더 추가는 2차로 미뤘다.
- 임시 서비스명은 `파이트 캘린더 코리아`다.
- UFC 공식 서비스로 오인되지 않도록 로고와 공식 사진을 사용하지 않고 비제휴 안내를 넣는다.
- 화면은 한국시간 기준이며 다음 대회와 이번 달 일정을 첫 화면에서 바로 보여준다.
- 자동 수집 결과는 즉시 공개하지 않고 검수 가능한 별도 결과로 만든다.
- 출처를 교체할 수 있도록 화면 데이터와 수집기를 분리한다.
- 사이트 제작 지침의 초기화 도구는 Windows 한글 경로 권한 문제로 실행되지 않아 같은 제공 템플릿을 직접 복사해 초기화했다.
- 인앱 미리보기 브라우저는 현재 환경에 제공되지 않아 빌드와 렌더링 테스트로 검증한다.
- 공식 UFC 대회 페이지로 검수한 다섯 개 대회를 초기 화면 데이터로 등록했다.
- 위키미디어 공개 API 수집기는 예정 대회를 검수 대기 파일로만 저장하며 화면에 자동 반영하지 않는다.
- 수집기 단위 테스트, 서버 렌더링 테스트, 프로덕션 빌드, 정적 검사를 통과했다.
- 링크 공유용 이미지는 공식 로고나 선수 사진 없이 별도 생성한 그래픽을 사용한다.
- 1차 버전을 비공개 운영 주소 `https://fight-calendar-korea.jjh020104.chatgpt.site`에 배포했다.
- 사용자 확인에서 `Uroš Medić` 특수문자 표기, 다음 경기 한국시간, 일정 화살표의 상세 이동 문제가 보고됐다.
- UFC 공식 이벤트 페이지의 최신 표시인 2026년 8월 1일 오후 1시 EDT를 기준으로 메인카드는 한국시간 8월 2일 일요일 오전 2시가 맞다.
- 선수명은 장치별 글꼴 호환을 위해 화면용 라틴 기본 문자로 표시한다.
- 일정 행 전체를 상세 영역으로 연결하고 모바일에서도 `상세 →` 버튼을 보이게 했다.
- 렌더링 테스트에서 메인카드 시각, 상세 링크와 대상, 선수명 표기를 함께 검증한다.
- 오류 수정 버전 2를 기존 비공개 운영 주소에 배포했다.
- 사용자 요청에 따라 모든 선수명은 한글을 크게 표시하고 영문 원명을 바로 아래에 작게 병기한다.
- 등록된 41개 대진의 82명 모두 한글 표시명을 추가했다.
- 메인이벤트, 일정 목록, 상세 대진표에서 동일한 한글 우선 이름 위계를 사용한다.
- 한글 선수명 버전 3을 기존 비공개 운영 주소에 배포했다.
- 사용자가 짧고 검색하기 쉬운 서비스명을 원해 공식 브랜드를 `FKO`로 확정했다.
- `FKO`는 `Fight Korea`와 `Fight + KO`를 함께 연상시키며 UFC 외 격투 종목으로 확장 가능한 이름이다.
- 화면 표기는 `FKO`를 주 브랜드로, `Fight Korea`를 보조 설명으로 사용한다.
- 슬로건은 `오늘의 격투기, 한 번에.`로 정했다.
- 검색 제목은 `FKO | UFC 한국시간 일정`로 설정하고 기존 운영 주소는 유지한다.
- FKO 이름, Fight Korea 보조 표기, 기존 일정 기능을 함께 검사하는 렌더링 테스트와 프로덕션 빌드를 통과했다.
- 공유 이미지는 공식 단체 로고나 선수 사진 없이 FKO와 일정표 모티프로 새로 제작했다.
- FKO 브랜드 버전 4를 기존 비공개 운영 주소에 배포하고 사이트 표시 제목도 `FKO`로 변경했다.
- 선수 정보는 모든 선수 이름에서 열 수 있게 하되 확인되지 않은 전적·랭킹을 추정하지 않는다.
- 주요 선수는 공식 UFC 프로필과 공식 랭킹을 우선 사용하고, 정보가 적은 선수는 이름과 해당 대진 체급만 표시한다.
- 사진은 재사용 권리를 확인할 수 있는 출처만 사용하며, 적합한 사진이 없으면 FKO 이니셜 그래픽으로 대체한다.
- 선수 정보는 별도 페이지보다 현재 대진을 보면서 확인하기 쉬운 화면 내 패널로 구현한다.
- 2026-07-29 기준 UFC 공식 선수 페이지와 공식 랭킹에서 주요 선수 15명의 전적·랭킹·기본 정보를 검수했다.
- 선수 이름은 상단 메인이벤트와 전체 대진표에서 모두 버튼으로 동작한다.
- 선수 패널은 전적, 공식 랭킹, 링네임, 출신, 스타일, 신장, 리치와 출처 확인일을 보여준다.
- 패널은 바깥 영역, 닫기 버튼, Esc 키로 닫을 수 있고 모바일에서는 하단형 정보 카드로 표시한다.
- 선수 정보 기능을 포함한 전체 렌더링 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 선수 정보 기능 버전 5를 기존 비공개 FKO 운영 주소에 배포했다.
- 대진표 전적은 `총 경기·승·패·무` 형식으로 통일하고 무승부가 없으면 생략한다.
- 공식 랭킹과 검수된 전적이 없는 선수는 빈 정보나 추정치를 표시하지 않는다.
- 최근 경기 결과는 선수 패널에서 승패, 상대, 날짜와 종료 방식을 함께 보여준다.
- 유튜브는 비공식 재업로드를 자동 수집하지 않고 UFC 공식 채널 안에서 선수명을 검색하는 링크를 제공한다.
- 다음 대회 메인카드에서 확인 가능한 추가 선수 5명의 공식 전적과 최근 결과를 보강해 상세 프로필을 20명으로 늘렸다.
- 대진표는 왼쪽 선수를 오른쪽 정렬하고 오른쪽 선수를 왼쪽 정렬해 VS 기준으로 대칭 배치했으며 가운데 간격을 넓혔다.
- 선수 이름 옆에는 공식 랭킹, 아래에는 `총 경기·승·패·무`를 표시하고 미검수 선수는 `전적 확인 중`으로 표시한다.
- 선수 패널에 최근 경기 결과 카드와 UFC 공식 유튜브 채널의 선수별 검색 링크를 추가했다.
- 대진 보강 기능을 포함한 전체 렌더링 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 대진 정보 보강 버전 6을 기존 비공개 FKO 운영 주소에 배포했다.
- 한국 선수 전용 코너는 사진 저작권 문제를 피하기 위해 기존 FKO 이니셜 그래픽을 유지한다.
- 2026-07-29 기준 UFC 공식 선수 페이지에서 현역으로 확인되는 박준용, 최두호, 박현성, 이정영, 유주상, 고석현 6명을 우선 제공한다.
- 한국 선수 카드는 홈에서 체급, 전적, 최근 결과와 다음 경기 상태를 요약하고 누르면 기존 선수 정보 패널을 연다.
- 공식 랭킹에 포함되지 않은 선수는 임의 순위를 붙이지 않고 `공식 랭킹 없음`으로 표시한다.
- 확인 가능한 다음 대진이 없는 선수는 `다음 경기 미정`으로 표시해 오래된 일정이나 소문을 노출하지 않는다.
- 선수 상세 정보에 소속 팀과 UFC 데뷔일을 추가하고 UFC 공식 선수 페이지 및 공식 유튜브 검색 링크를 유지했다.
- 한국 선수 코너를 포함한 서버 렌더링 테스트, 수집기 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 한국 선수 집중 코너 버전 7을 기존 비공개 FKO 운영 주소에 배포했다.
- 사용자가 요청한 GitHub 저장소는 `https://github.com/hyunjun328/FKO.git`이며 확인 시점에 브랜치가 없는 빈 저장소였다.
- 기존 Sites 배포용 `origin`은 유지하고 GitHub 저장소는 `github`라는 별도 원격으로 연결한다.
- FKO 전체 소스를 `hyunjun328/FKO`의 `main` 브랜치에 올리고 원격 커밋 일치를 확인했다.
- 고석현 대 장폴 레보스노야니 결과는 UFC 공식 이벤트 결과, UFC 공식 스코어카드와 승자 인터뷰, Sherdog에서 장폴의 만장일치 판정승으로 일치한다.
- 기존 오류는 UFC 선수 프로필의 카드형 `Win` 표시를 선수 이름과 잘못 대응한 데서 발생했다. 프로필 본문과 이벤트 결과가 충돌할 때 프로필 카드 표시를 근거로 사용하지 않는다.
- 고석현의 경기 후 전적은 13승 3패이며 최근 결과는 2026-07-18 장폴 레보스노야니전 만장일치 판정패다.
- 고석현은 UFC 공식 경기 전 인터뷰에서 링네임을 `The Korean Tyson`에서 `Technical`로 변경했다고 밝혔다.
- 앞으로 결과는 공식 이벤트 결과와 공식 스코어카드를 우선하고 Sherdog 또는 체육위원회 결과처럼 선수 배열과 무관한 독립 출처를 한 곳 이상 추가 대조한다.
- 한국 선수 6명 모두 프로필, 공식 이벤트 결과와 독립 전적 출처를 다시 대조하고 각 프로필에 검수 링크를 연결했다.
- 고석현은 공식 결과, 공식 스코어카드, 공식 승자 인터뷰, 공식 고석현 인터뷰와 Sherdog 등 5개 근거를 화면에서 직접 확인할 수 있다.
- 고석현 카드가 `패`, `13승 3패`, `Technical`, `장폴 레보스노야니`를 함께 렌더링하는 회귀 테스트를 추가했다.
- 데이터 수정 후 서버 렌더링 테스트, 수집기 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- UFC 데이터 검수 강화 버전 8을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 현재 수집기는 예약 실행되지 않으며 `scripts/collect_wikipedia.py`를 직접 실행할 때만 검수 대기 데이터를 만든다.
- Codex 대화가 끝난 뒤 모델이 백그라운드에서 계속 수집하는 구조도 아니다.
- 자동화 시 권장 주기는 평상시 6시간마다, 경기 72시간 전부터 1시간마다 수집하되 출처 간 일치가 확인된 정보만 공개하는 방식이다.
- 홈 상단 카운트다운은 제거하고 실시간 한국시간과 다음 주요 넘버링 대회의 메인이벤트를 함께 보여준다.
- 현재 한국시간은 분 단위로 자동 갱신하고 날짜와 요일을 함께 표시한다.
- 다음 주요 매치는 가장 가까운 예정 넘버링 UFC 대회를 자동 선택하며 누르면 해당 대회 상세로 이동한다.
- 카운트다운 제거와 한국시간·주요 매치 렌더링을 검사하는 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 홈 상단 정보 개선 버전 9를 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- GitHub Pages `https://hyunjun328.github.io/FKO/`는 앱 빌드 결과가 아니라 `main` 브랜치 루트의 README를 Jekyll로 변환한 문서를 제공하고 있었다.
- FKO 앱은 Cloudflare Worker 호환 서버 빌드라 GitHub Pages의 정적 Jekyll 배포에서 직접 실행되지 않는다.
- GitHub에는 코드를 보관하고 실제 서비스는 기존 Sites 주소에서 단일 운영하며, Pages 진입 주소는 운영 사이트로 안내하는 방식이 데이터 중복과 배포 불일치를 막는 가장 단순한 해결책이다.
- 코리안 파이터는 별도 `/korean-fighters` 화면으로 분리하고 홈에는 짧은 진입 안내만 남긴다.
- 전용 화면은 현재 제공 중인 현역 6명을 먼저 보여주고, 아래에 정찬성·김동현·강경호·정다운·임현규·양동이 등 은퇴 또는 전 UFC 선수를 구분해 보여준다.
- 과거 선수의 상태는 모두 `은퇴`로 단정하지 않고 UFC 공식 페이지의 `Not Fighting`, 공식 은퇴 기사, 최근 전적 자료를 근거로 `은퇴·전 UFC` 범주로 묶는다.
- 공용 선수 카드와 상세 정보 창을 분리해 홈 일정과 코리안 파이터 전용 화면에서 같은 검수 데이터를 사용한다.
- 홈의 6개 선수 카드 목록은 현역 6명·역대 6명 전용 화면으로 이동하는 안내 카드로 교체했다.
- 전용 화면에는 현역 6명 아래에 정찬성, 김동현, 강경호, 정다운, 임현규, 양동이 6명을 추가했다.
- GitHub Pages 루트에는 현재 운영 중인 FKO 주소로 이동하는 안내 페이지를 추가하고 하위 경로도 같은 운영 경로로 연결한다.
- 홈과 `/korean-fighters` 서버 렌더링 테스트 2개, 수집기 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 코리안 파이터 전용 화면 버전 10을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- GitHub Pages 루트가 Jekyll README 문서 대신 `FKO로 이동 중` 안내 페이지를 제공하는 것을 공개 주소에서 확인했다.
- 최두호 대 파트리시우 핏불은 UFC 331의 2026-09-19 로스앤젤레스 대진으로 복수 전문 매체가 보도했고 최두호도 대진을 확인했다.
- 현재 UFC 공식 일정 페이지에는 아직 대진과 시작 시각이 게시되지 않았으므로 `공식 확정`이 아니라 `대진 보도 확인 · UFC 공식 발표 전`으로 표시한다.
- 로스앤젤레스 현지 2026-09-19 대회는 한국 날짜로 2026-09-20이며 구체적인 시작 시각은 미정으로 둔다.
- 기존 오류는 현역 카드가 선수별 다음 경기 데이터 없이 `다음 경기 미정`을 고정 출력했기 때문에 발생했다.
- 선수 프로필에 선택형 `nextFight` 데이터를 추가하고 최두호 카드와 상세 정보 창에 상대, 대회, 한국 날짜, 시작 시각 상태와 보도 근거를 표시했다.
- 다른 현역 선수는 확인 가능한 대진이 없을 때만 `다음 경기 미정`을 유지한다.
- 최두호 카드가 파트리시우 핏불, UFC 331, 2026-09-20 KST와 UFC 공식 발표 전 상태를 렌더링하고 `다음 경기 미정`을 포함하지 않는 회귀 테스트를 통과했다.
- 전체 서버 렌더링 테스트, 수집기 테스트, 프로덕션 빌드와 정적 검사를 통과했다.
- 최두호 다음 경기 표시 수정 버전 11을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 사용자는 메인 상단의 실시간 한국시간 시계가 불필요하다고 판단했다. 일정의 KST 표기는 유지하고 라이브 시계 카드만 제거한다.
- 정보 위계는 `메인이벤트 선수명 > 메인카드 시작 시각 > 다음 대회명` 순으로 조정한다.
- 라이브 시계 관련 포매터와 카드 스타일을 제거하고 오른쪽 패널은 다음 주요 매치 전용으로 정리했다.
- 서버 렌더링 테스트 2개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다. 연결 가능한 브라우저가 없어 자동 스크린샷 검사는 진행하지 못했다.
- 홈 경기 정보 위계 개선 버전 12를 기존 비공개 FKO 운영 주소에 배포하고 같은 수정 커밋을 GitHub에 반영했다.
- 새 시각 방향은 검정 바탕, 흰 글씨, 빨강과 노랑 강조색이다. 녹색·연두색 상태 표현은 노랑 또는 빨강으로 통합한다.
- 선수 사진은 실존 인물을 AI로 생성하지 않는다. 공식 또는 사용 허가가 확인된 사진만 향후 추가하고 그전에는 일관된 FKO 그래픽을 사용한다.
- 전역 색상은 검정 `#050505`, 흰색 `#f7f7f2`, 빨강 `#ff3434`, 노랑 `#ffd21f`를 기준으로 통일했다.
- 기존 초록 상태색은 노랑으로, 파란 토요일 표시는 노랑으로 교체하고 선택·패배·VS 표시는 빨강으로 정리했다.
- 실제 선수 얼굴을 넣지 않은 1200×630 FKO 소셜 공유 이미지를 새 테마에 맞춰 생성하고 `og-dark.png`로 연결했다.
- 서버 렌더링 테스트 3개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다.
- 검정·빨강·노랑 테마 버전 13을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정 커밋을 GitHub에 반영했다.
- UFC는 2026-06-22부터 미디어 투표 랭킹에서 경기 데이터 기반 Meta UFC 랭킹으로 전환 중이며 현재 공식 랭킹 페이지에 두 목록을 함께 게시한다.
- Meta UFC 랭킹은 상대 수준, 승리 방식, 경기 시점, 최근 흐름, 미활동과 체급 특성을 반영하지만 정확한 점수식과 가중치는 공개하지 않았다.
- FKO는 공식 Meta 결과를 그대로 기본 순위로 제공하고 기존 미디어 순위는 비교용으로만 표시한다. 독자적인 역산이나 15위 밖 추정은 하지 않는다.
- `/rankings` 화면에 남녀 11개 체급의 챔피언과 Meta·미디어 각 1~15위를 나란히 표시하고, Meta 순위 옆에 미디어 대비 차이를 계산해 보여준다.
- 랭킹 화면은 검정 배경, 흰 글씨, 빨강·노랑 강조색을 유지하며 모바일에서는 두 순위표를 세로로 배치한다.
- UFC 공식 갱신일, FKO 대조일, UFC 랭킹 원문과 Meta 도입 발표 링크를 화면에 함께 표시한다.
- 서버 렌더링 테스트는 11개 체급의 총 330개 순위 행과 대표 챔피언, 특수문자가 포함된 선수명, 출처 링크를 검증한다.
- 전체 서버 렌더링 테스트 4개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다. 연결 가능한 브라우저가 없어 자동 스크린샷 검사는 진행하지 못했다.
- UFC 공식 선수 랭킹 화면 버전 14를 기존 비공개 FKO 운영 주소에 배포하고 기능 커밋을 GitHub에 반영했다.
- 사용자는 현재 화면이 큰 글자, 강한 색과 장식이 많아 화려하지만 신뢰감이 부족하고 AI로 빠르게 만든 인상이라고 판단했다.
- 새 방향은 검정 테마를 유지하면서 빨강·노랑을 상태와 핵심 정보에만 쓰고, 작은 모서리·얇은 경계선·절제된 제목 크기로 스포츠 데이터 서비스처럼 정돈하는 것이다.
- 화면 구조와 기능은 유지하고 시각 체계만 수술적으로 바꾼다. 실제 사진이나 새로운 기능은 이번 범위에 포함하지 않는다.
- 배경 방사형 그라데이션, 기울어진 노란 로고, 다음 경기 워터마크, 빨간 오프셋 그림자와 카드 이동 효과를 제거했다.
- 페이지 제목의 최대 크기와 굵기를 낮추고 영문 장식 라벨을 한국어 정보 라벨로 바꿨다.
- 홈, 일정 상세, 선수 카드, 선수 정보 창과 랭킹 표를 5~10px 모서리와 얇은 회색 경계선 기준으로 통일했다.
- 빨강은 선택과 승부, 노랑은 시각과 검수 상태에만 남기고 일반 링크·표·안내 카드는 회색 계열로 낮췄다.
- 서버 렌더링 테스트 4개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다.
- 신뢰도 중심 디자인 정돈 버전 15를 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 사용자는 랭킹 화면에서 한글 이름 아래에 영문 이름이 함께 보이기를 원하며, 한글·영문 선수명 검색 기능을 요청했다.
- 챔피언은 체급 제목 오른쪽 보조 상자가 아니라 해당 체급 순위표의 가장 먼저 읽히는 위치에 배치한다.
- 전체 타이포그래피는 900~950 굵기의 반복을 줄이고, 한국어 가독성과 격투 스포츠 특유의 단단한 인상을 함께 살리는 방향으로 바꾼다.
- 현재 랭킹에 등장하는 챔피언과 선수의 중복 제거 기준 200명 모두 한글 이름을 연결했고 누락 검사 테스트를 추가했다.
- 랭킹 검색은 한글 이름, 영문 이름과 한글·영문 체급명을 지원하며 검색 버튼과 전체 랭킹 초기화를 제공한다.
- 챔피언은 체급 헤더의 오른쪽 상자에서 제거하고 두 순위표 위의 전체 너비 행으로 옮겼다.
- 각 순위 행은 한글 이름을 먼저 표시하고 바로 아래에 UFC 공식 영문 표기를 보조 정보로 유지한다.
- Pretendard Variable 글꼴과 OFL 라이선스를 사이트에 직접 포함해 외부 글꼴 서버 없이 같은 화면을 제공한다.
- 전체 제목은 700, 선수명은 650, 보조 라벨은 560 안팎으로 낮췄다.
- 서버 렌더링 테스트 5개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다.
- 랭킹 검색은 특정 선수와 같은 체급이라는 이유만으로 무관한 챔피언을 보여주지 않아야 한다.
- 랭킹 밖 코리안 파이터는 임의 순위를 붙이지 않고 `UFC 공식 랭킹 없음`으로 검색 결과에 제공한다.
- 챔피언은 공식 1~15위와 별도이므로 검색 시 Meta와 미디어 양쪽에 `현 UFC 챔피언` 상태로 표시한다.
- 최두호·찰스 올리베이라·저스틴 게이치 검색 회귀 테스트를 추가했고 전체 8개 테스트, 린트와 프로덕션 빌드를 통과했다.
- 랭킹 검색 정확도 개선 버전 18을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 사용자는 ChatGPT 로그인이 필요한 비공개 Sites 주소 대신 로그인 없는 GitHub Pages 공개 주소를 원한다.
- 기존 ChatGPT Sites 배포는 예비 주소로 보존하고 GitHub Pages를 새 공개 진입점으로 추가한다.
- GitHub Pages는 정적 파일만 제공하므로 현재 App Router 화면을 Next.js `output: export`로 별도 빌드한다.
- Pages 전용 빌드는 `/FKO` basePath, 정적 내보내기, 글꼴 경로 보정과 `.nojekyll` 생성을 한 번에 처리한다.
- 홈·랭킹·코리안 파이터 정적 페이지 3개, 하위 경로 링크, 스크립트·글꼴·공유 이미지 경로를 자동 검증한다.
- 기존 Vinext 테스트 8개와 수집기 테스트, Pages 테스트 2개, 린트를 통과했다.
- GitHub Pages를 workflow 방식으로 활성화했고 `https://hyunjun328.github.io/FKO/`의 홈·랭킹·코리안 파이터가 비로그인 요청에 모두 200으로 응답한다.
- 공개 주소의 Pretendard 글꼴과 Next.js 화면 동작 파일도 각각 200으로 제공되며 이후 main 브랜치 갱신 시 자동 재배포된다.
- 선수 사진은 UFC 공식 이미지 무단 복사 대신 Wikimedia Commons의 파일별 재사용 조건을 확인해 사용한다.
- 이미지 파일은 사이트에 내려받아 작은 WebP로 제공하고 사진별 저작자·라이선스·원본 링크를 별도 출처 화면에 표시한다.
- 메인카드는 일정 데이터의 `section: main`, 한국 선수는 현역·역대 목록, 랭킹은 챔피언과 Meta·미디어 1~15위를 대상으로 한다.
- 선수 219명을 자동 추출했고 본인 여부와 재사용 라이선스를 확인할 수 있는 Wikimedia Commons 사진 110장을 확보했다.
- 자유 이용 사진이 확인되지 않은 109명은 잘못된 인물이나 무단 이미지를 넣지 않고 같은 크기의 이니셜 대체 UI를 사용한다.
- 사진은 최대 360px WebP 110개, 총 약 1.15MB로 저장했고 일반 목록에서는 지연 로딩한다.
- `/photo-credits`에서 선수별 저작자, 라이선스와 Commons 원본 파일을 공개한다.
- 서버 화면 테스트 9개, Python 수집기 테스트 3개, Pages 테스트 2개와 린트를 통과했다.
- 선수 사진 적용 커밋 `780872f`를 GitHub main에 반영했고 Pages 빌드와 배포 작업이 모두 성공했다.
- 공개 홈, 랭킹, 코리안 파이터, 사진 출처와 실제 WebP 파일이 모두 200으로 응답하는 것을 확인했다.
- 사용자는 Commons 자동 수집 사진의 구도와 품질이 낮고 원형 프레임도 어울리지 않는다고 판단했다.
- UFC 이용약관은 사진을 포함한 자료의 무단 복제·재게시와 자동 수집을 제한하므로 공식 프로필 사진은 서면 허락 없이 사이트에 복사하지 않는다.
- 사진 노출 기준은 한 명의 얼굴을 알아볼 수 있는 구도, 충분한 해상도와 자연스러운 세로형 크롭으로 강화한다.
- 검색 API 결과를 페이지 순서가 아닌 내부 ID 순서로 읽어 디에고 로페스에 브라이언 오르테가 사진이 연결되는 잘못된 매칭이 발생했다.
- 검색 결과를 원래 검색 순서로 정렬하고 선수명과 문서 제목 유사도가 82% 이상일 때만 사진을 받도록 수정했다.
- 사진 110장을 접촉표로 전수 검토했고 잘못된 매칭을 제거한 뒤 저화질 셀카·영상 캡처·얼굴이 작은 사진 21장을 추가 제외해 81장만 남겼다.
- 모든 선수 사진과 이니셜 대체 UI를 원형에서 세로형 사각 프레임으로 바꿨다.
- 서버 화면 테스트 9개, Python 수집기 테스트 4개, Pages 테스트 2개와 린트를 통과했다.
- 사용자는 맥그리거처럼 유명한 선수에게 챔피언 경력, 흥행 기록과 간판스타 같은 맥락을 더 자세히 제공하길 원한다.
- 위키백과 단일 출처의 서술을 그대로 복사하지 않고 UFC 공식 자료와 독립 출처를 함께 확인한 사실만 커리어 핵심 정보로 넣는다.
- UFC 공식 랭킹은 체급별 15위까지만이므로 그 밖의 선수는 `UFC 공식 NR`로 유지하고 외부 세계 순위는 `비공식`과 기준일을 함께 표시한다.
- 현재 대진표에 등장하는 선수는 공식 랭킹 여부와 관계없이 검색과 프로필 진입을 지원한다.
- 대진 데이터에서 선수 100여 명을 자동으로 중복 제거해 검색 목록을 만들므로 새 대진을 추가하면 랭킹 검색에도 함께 나타난다.
- 맥그리거, 하빕, 조르주 생피에르, 아만다 누네스의 대표 커리어를 UFC 공식 프로필·공식 회고와 독립 기록 출처로 검수해 상세창에 넣었다.
- 맥그리거의 동시 두 체급 챔피언 경력은 UFC 공식 자료, UFC 229의 추정 240만 PPV 기록은 기네스 세계기록에서 확인했다.
- Fight Matrix 2026-07-26 발표분을 UFC 공식 랭킹과 분리해 우로시 메디치 18위, 다니엘 로드리게스 19위, 미크티벡 오롤바이 23위 등으로 표기한다.
- Fight Matrix 순위는 여러 단체를 합친 비공식 세계 순위이며 `UFC 공식 NR`과 제공처·기준일을 항상 함께 보여준다.
- 대진표 선수에게 검수 프로필이 없어도 선수 이름을 누르면 기본 상세창과 확인된 비공식 순위가 열린다.
- 서버 화면·검색 회귀 테스트 12개, Python 수집기 테스트 4개, Pages 정적 내보내기 테스트 2개와 린트를 통과했다.
- 기능 커밋 `5da5242`를 GitHub main에 반영했고 Pages 작업 `30502557350`의 빌드와 배포가 모두 성공했다.
- 공개 랭킹 페이지가 200으로 응답하며 전체 대진표 검색 안내와 Fight Matrix 비공식 순위 안내가 실제 HTML에 반영된 것을 확인했다.
- 상단 메뉴 가시성 개선 버전 17을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 랭킹 검색·한글 이름·타이포그래피 개선 버전 16을 기존 비공개 FKO 운영 주소에 배포하고 같은 수정본을 GitHub에 반영했다.
- 사용자는 우측 상단의 선수 랭킹과 코리안 파이터 링크가 작아 찾기 어렵고, 홈 하단의 코리안 파이터 진입 영역은 중복이라고 판단했다.
- 두 기능의 기본 진입점은 상단 메뉴로 통일하고 홈 하단 코리안 파이터 섹션은 제거한다.
- 상단 메뉴는 데스크톱 40px, 모바일 38px 높이의 테두리 버튼으로 바꾸고 모바일의 기존 숨김 규칙을 해제했다.
- 모바일에서는 일정 검수 상태 문구를 숨겨 선수 랭킹과 코리안 파이터 메뉴의 터치 영역을 우선한다.
- 홈 하단의 현역 6명·역대 6명 소개 카드와 더 이상 사용하지 않는 전용 스타일을 제거했다.
- 서버 렌더링 테스트 5개, 수집기 테스트, 프로덕션 빌드와 린트를 통과했다.
