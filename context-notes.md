# 작업 맥락 기록

## 2026-07-29

- 기존 뉴스 프로그램은 유지하고 `fight-calendar`라는 별도 프로젝트로 분리했다.
- 1차는 일정 확인 도구에 집중한다.
- 로그인, 랜덤 아이디, 승부예측, 댓글, 순위, 관심 경기, 스포일러, 외부 캘린더 추가는 2차로 미뤘다.
- 임시 서비스명은 `파이트 캘린더 코리아`다.
- UFC 공식 서비스로 오인되지 않도록 로고와 공식 사진을 사용하지 않고 비제휴 안내를 넣는다.
- 화면은 한국시간 기준이며 다음 대회와 이번 달 일정을 첫 화면에서 바로 보여준다.
- 자동 수집 결과는 즉시 공개하지 않고 검수 가능한 별도 결과로 만든다.
- 출처를 교체할 수 있도록 화면 데이터와 수집기를 분리한다.
- 사이트 제작 지침의 초기화 도구는 Windows 한글 경로 권한 문제로 실행되지 않아 같은 제공 템플릿을 직접 복사해 초기화했다.
- 인앱 미리보기 브라우저는 현재 환경에 제공되지 않아 빌드와 렌더링 테스트로 검증한다.
- 공식 UFC 대회 페이지로 검수한 다섯 개 대회를 초기 화면 데이터로 등록했다.
- 위키미디어 공개 API 수집기는 예정 대회를 검수 대기 파일로만 저장하며 화면에 자동 반영하지 않는다.
- 수집기 단위 테스트, 서버 렌더링 테스트, 프로덕션 빌드, 정적 검사를 통과했다.
- 링크 공유용 이미지는 공식 로고나 선수 사진 없이 별도 생성한 그래픽을 사용한다.
- 1차 버전을 비공개 운영 주소 `https://fight-calendar-korea.jjh020104.chatgpt.site`에 배포했다.
- 사용자 확인에서 `Uroš Medić` 특수문자 표기, 다음 경기 한국시간, 일정 화살표의 상세 이동 문제가 보고됐다.
- UFC 공식 이벤트 페이지의 최신 표시인 2026년 8월 1일 오후 1시 EDT를 기준으로 메인카드는 한국시간 8월 2일 일요일 오전 2시가 맞다.
- 선수명은 장치별 글꼴 호환을 위해 화면용 라틴 기본 문자로 표시한다.
- 일정 행 전체를 상세 영역으로 연결하고 모바일에서도 `상세 →` 버튼을 보이게 했다.
- 렌더링 테스트에서 메인카드 시각, 상세 링크와 대상, 선수명 표기를 함께 검증한다.
- 오류 수정 버전 2를 기존 비공개 운영 주소에 배포했다.
